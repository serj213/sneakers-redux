import React from "react";
import { useSelector } from "react-redux";


const {basketCarts} = useSelector(state => state.basket);

console.log('dfdf');

export const TotalPriceSum = () => {
    const totalPrice = 0;




}

